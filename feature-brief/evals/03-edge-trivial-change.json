{
  "$schema": "skill-eval/v1",
  "name": "edge-trivial-change",
  "skill": "feature-brief",
  "query": "Danny asked me to rename a config property in application.yml",
  "files": [],
  "expected_behavior": [
    "States it is skipping the brief because the change is trivial mechanical",
    "Does the change directly without creating a brief file"
  ],
  "should_NOT_trigger": false
}
